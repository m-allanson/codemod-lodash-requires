{
  "name": "codemod-lodash-requires",
  "version": "0.0.1",
  "description": "Codemod lodash requires from \"full import\" to \"single method\" style in CommonJS modules",
  "main": "",
  "scripts": {
    "test": "jest",
    "format": "npm run prettier \"transforms/**\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^25.5.4",
    "jscodeshift": "^0.7.0"
  }
}
